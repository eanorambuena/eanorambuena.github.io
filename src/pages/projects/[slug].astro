---
import Layout from '../../layouts/Layout.astro'
import AnimatedSection from '../../components/AnimatedSection.jsx'
import { es, en } from '../../i18n/translations'

export async function getStaticPaths() {
  const slugs = ['neuralworks', 'smartlokus', 'loopaas', 'kalmate']
  return slugs.map((slug) => ({ params: { slug } }))
}

const { slug } = Astro.params

const projects: Record<string, any> = {
  neuralworks: {
    titleEs: 'NeuralWorks — Fullstack POS',
    titleEn: 'NeuralWorks — Fullstack POS',
    tags: ['React Native', 'TypeScript', 'AWS', 'Tamagui', 'Docker', 'Expo'],
    image: '/projects/neural-app.png',
    costImage: '/projects/neural-costs.png',
    color: 'from-emerald-500 to-teal-500',
    metrics: [
      { value: '$200→$0', labelEs: 'Costos IA/mes', labelEn: 'AI costs/mo' },
      { value: '500+', labelEs: 'Usuarios concurrentes', labelEn: 'Concurrent users' },
      { value: '7.2→5.9GB', labelEs: 'Swap de memoria', labelEn: 'Memory swap' },
    ],
    featuresEs: [
      'Migración UI de React Native Paper a Tamagui con Design Tokens',
      'Módulo Fullstack de control y edición de inventario (POS)',
      'Infraestructura cloud con AWS, SST, Pulumi, Hono y Drizzle ORM',
      'Reducción de Swap de memoria de 7.28GB a 5.9GB con Docker',
      'Implementación de Cursor Rules: costos IA de >$200/mes a $0',
      'Transición a Atomic PRs para eliminar merge conflicts',
      'Componentes reutilizables, Custom Hooks y Zustand stores',
      'Sistema de puntos, historial de compras y filtrado por email',
    ],
    featuresEn: [
      'UI migration from React Native Paper to Tamagui with Design Tokens',
      'Fullstack inventory control and editing module (POS)',
      'Cloud infrastructure with AWS, SST, Pulumi, Hono, and Drizzle ORM',
      'Memory Swap reduction from 7.28GB to 5.9GB with Docker',
      'Cursor Rules implementation: AI costs from >$200/mo to $0',
      'Atomic PRs transition to eliminate merge conflicts',
      'Reusable components, Custom Hooks, and Zustand stores',
      'Points system, purchase history, and email filtering',
    ],
    longDescEs: 'Trabajé como Fullstack Software Engineer en el proyecto HOP de NeuralWorks, un Point of Sale (POS) para Tablets. Lideré la migración de la UI legacy de React Native Paper a Tamagui, diseñé e implementé el módulo completo de inventario (cliente + servidor), y optimicé la infraestructura cloud con AWS, SST y Docker. También implementé Cursor Rules que redujeron los costos de IA de >$200/mes a $0, y establecí flujos de Atomic PRs para eliminar cuellos de botella en el equipo.',
    longDescEn: 'Worked as a Fullstack Software Engineer on NeuralWorks internal HOP project, a Point of Sale (POS) for Tablets. Led the UI migration from legacy React Native Paper to Tamagui, designed and implemented the complete inventory module (client + server), and optimized cloud infrastructure with AWS, SST and Docker. Also implemented Cursor Rules that reduced AI costs from >$200/mo to $0, and established Atomic PR workflows to eliminate team bottlenecks.',
    links: [],
    testimonials: [
      {
        author: 'Sofía Schwarzenberg',
        role: 'Technical Product Manager @ NeuralWorks',
        textEs: '"Emmanuel ha demostrado un muy buen desempeño durante su práctica. Trabaja con autonomía, entrega código de alta calidad y logra abordar tareas complejas. No se limita a implementar lo asignado: toma iniciativa para mejorar el código existente y crear componentes reutilizables que benefician a todo el equipo."',
        textEn: '"Emmanuel demonstrated very good performance during his internship. He works autonomously, delivers high-quality code, and tackles complex tasks. He does not limit himself to implementing what is assigned: he takes initiative to improve existing code and create reusable components that benefit the entire team."',
      },
    ],
  },
  smartlokus: {
    titleEs: 'Smartlokus: GIS para Minería',
    titleEn: 'Smartlokus: GIS App for Mining',
    tags: ['React', 'TypeScript', 'GIS', 'Vite'],
    iframe: 'https://procel.acofuz.com/login',
    color: 'from-orange-500 to-red-500',
    metrics: [
      { value: '100%', labelEs: 'Frontend desde cero', labelEn: 'Scaffolded from scratch' },
      { value: 'GIS', labelEs: 'Visualización minera', labelEn: 'Mining visualization' },
    ],
    testimonials: [
      {
        author: 'Jorge Araneda',
        role: 'Tech Lead @ Gosocket / Co-Founder Acofuz',
        textEs: '"Emma es de las personas que cuando conversas con él te das cuenta que vale la pena. Tiene un gran conocimiento pero comparte su conocimiento con el resto del equipo, enseñando de una manera agradable y clara. Es de las personas que dice que hará algo y realmente lo cumple."',
        textEn: '"Emma is one of those people that when you talk to him, you realize he is worth it. He has great knowledge but shares it with the rest of the team, teaching in a pleasant and clear way. He is the kind of person who says he will do something and actually does it."',
      },
      {
        author: 'Compañero de equipo',
        role: 'Acofuz',
        textEs: '"Trabajar en el frontend de Smartlokus fue muy fácil gracias a lo extensible y ordenado del código base, gracias a Emma."',
        textEn: '"Working on the Smartlokus frontend was very easy thanks to how extensible and clean the codebase was, thanks to Emma."',
      },
    ],
    featuresEs: [
      'Frontend scaffolded desde cero, escalable y mantenible',
      'Integración vía API con backend Python',
      'Comunicación constante con equipo backend para integraciones',
      'Visualización de datos geoespaciales con mapas interactivos',
      'Arquitectura extensible para nuevas funcionalidades',
    ],
    featuresEn: [
      'Frontend scaffolded from scratch, scalable and maintainable',
      'API integration with Python backend',
      'Constant collaboration with backend team',
      'Geospatial data visualization with interactive maps',
      'Extensible architecture for future features',
    ],
    longDescEs: 'Aplicación GIS de visualización de datos geoespaciales construida para Acofuz, utilizada por Procel y Antofagasta Minerals. Frontend desarrollado desde cero con React, TypeScript y Vite, con arquitectura escalable y mantenible. Integración vía API con backend Python para procesamiento de datos geoespaciales.',
    longDescEn: 'GIS visualization app built for Acofuz, used by Procel and Antofagasta Minerals. Frontend built from scratch with React, TypeScript, and Vite, with scalable and maintainable architecture. API integration with Python backend for geospatial data processing.',
    links: [{ labelEs: 'Web →', labelEn: 'Web →', url: 'https://procel.acofuz.com/login' }],
  },
  loopaas: {
    titleEs: 'Loopaas: Coevaluaciones Universitarias',
    titleEn: 'Loopaas: Peer Evaluation Platform',
    tags: ['Next.js', 'React', 'PostgreSQL', 'Supabase', 'Auth0'],
    iframe: 'https://idsapp.vercel.app/',
    color: 'from-green-500 to-teal-500',
    metrics: [
      { value: '1,500+', labelEs: 'Estudiantes', labelEn: 'Students' },
      { value: '60%', labelEs: 'Menos tiempo corrección', labelEn: 'Less grading time' },
      { value: '16GB', labelEs: 'RAM ya no satura', labelEn: 'RAM no longer saturated' },
    ],
    featuresEs: [
      'Evaluaciones peer-to-peer anónimas',
      'Crear, editar y eliminar criterios y sus porcentajes',
      'Control de asistencia por escaneo QR',
      'SSO con Supabase y Auth0',
      'Generación de informes de notas descargables',
      'SEO optimizado',
      'Dashboard en tiempo real para profesores',
      'Reemplazo de Google Forms + GScript que saturaba PCs con 16 GB RAM',
    ],
    featuresEn: [
      'Anonymous peer-to-peer evaluations',
      'Create, edit, and delete criteria with custom percentages',
      'QR code attendance tracking',
      'SSO with Supabase and Auth0',
      'Grade report generation',
      'SEO optimized',
      'Real-time dashboard for professors',
      'Replaced Google Forms + GScript that saturated PCs with 16 GB RAM',
    ],
    longDescEs: 'Plataforma de coevaluaciones universitarias. Los estudiantes se evalúan entre sí de forma anónima con criterios personalizables. Incluye control de asistencia por QR, SSO con Supabase y Auth0, informes de notas descargables y SEO optimizado. Originalmente era un Google Form cuyo procesamiento con GScript saturaba cualquier PC, incluso con 16 GB de RAM. Construido con Next.js, React, PostgreSQL, Supabase y Auth0.',
    longDescEn: 'Peer evaluation platform. Students evaluate each other anonymously with customizable criteria. Includes QR attendance tracking, SSO with Supabase and Auth0, downloadable grade reports, and SEO optimization. Originally a Google Form whose GScript processing saturated any PC, even with 16 GB of RAM. Built with Next.js, React, PostgreSQL, Supabase, and Auth0.',
    links: [{ labelEs: 'Web →', labelEn: 'Web →', url: 'https://idsapp.vercel.app/' }],
  },
  kalmate: {
    titleEs: 'Kalmate: Terminal Financiera',
    titleEn: 'Kalmate: Financial Terminal',
    tags: ['Nuxt 3', 'Vue', 'TypeScript', 'Yahoo Finance', 'TradingView'],
    iframe: 'https://kalmate.pages.dev',
    color: 'from-blue-500 to-cyan-500',
    metrics: [
      { value: '6', labelEs: 'Mercados en vivo', labelEn: 'Live markets' },
      { value: '30s', labelEs: 'Cache de quotes', labelEn: 'Quote cache' },
      { value: '0', labelEs: 'API keys necesarias', labelEn: 'API keys needed' },
    ],
    featuresEs: [
      'Datos de mercado en tiempo real vía Yahoo Finance API',
      'Dashboard categorizado: Equities, Forex, Bonds, Commodities, Crypto',
      'Portafolio con seguimiento de P&L',
      'Alertas inteligentes con notificaciones del navegador',
      'Screener con filtros, presets y ordenamiento',
      'Noticias financieras integradas',
      'Cacheo inteligente del servidor (30s quotes, 5min history)',
      'Atajos de teclado para navegación eficiente',
      'Pipeline visual de nodos: conecta inputs (Symbol, Currency, Price Feed) con procesadores (Kalman Filter, SMA, RSI, EMA, Forecast) y outputs (Chart, Alert, Telegram, Email)',
      'Ejecución DAG con orden topológico y caché inteligente por nodo',
    ],
    featuresEn: [
      'Real-time market data via Yahoo Finance API',
      'Categorized dashboard: Equities, Forex, Bonds, Commodities, Crypto',
      'Portfolio with P&L tracking',
      'Smart alerts with browser notifications',
      'Screener with filters, presets, and sorting',
      'Integrated financial news',
      'Intelligent server caching (30s quotes, 5min history)',
      'Keyboard shortcuts for efficient navigation',
      'Automated visual data pipeline: connect inputs (Symbol, Currency, Price Feed) with processors (Kalman Filter, SMA, RSI, EMA, Forecast) and outputs (Chart, Alert, Telegram, Email)',
      'DAG execution engine with topological ordering and per-node caching',
    ],
    longDescEs: 'Terminal financiera estilo Bloomberg con datos de mercado en tiempo real vía Yahoo Finance API. Construido con Nuxt 3, Vue 3, TypeScript y TradingView charts. Dashboard categorizado (Equities, Forex, Bonds, Commodities, Crypto), portafolio con P&L, alertas inteligentes con notificaciones del navegador, screener con filtros y presets, noticias financieras, y pipeline visual de nodos para análisis de datos (Filtro de Kalman, SMA, RSI, EMA, Forecast). Cacheo inteligente del servidor y atajos de teclado para navegación eficiente. Sin necesidad de API keys.',
    longDescEn: 'Bloomberg-style financial terminal with real-time market data via Yahoo Finance API. Built with Nuxt 3, Vue 3, TypeScript, and TradingView charts. Categorized dashboard (Equities, Forex, Bonds, Commodities, Crypto), portfolio with P&L tracking, smart alerts with browser notifications, screener with filters and presets, financial news, and a visual node pipeline for data analysis (Kalman Filter, SMA, RSI, EMA, Forecast). Intelligent server caching and keyboard shortcuts for efficient navigation. No API keys required.',
    links: [{ labelEs: 'Web →', labelEn: 'Web →', url: 'https://kalmate.pages.dev' }],
  },
}

const project = projects[slug as keyof typeof projects]

if (!project) {
  return Astro.redirect('/404')
}
---

<Layout title={`${project.titleEs} | Emmanuel Norambuena`} description={project.longDescEs} ogImage={project.image ? new URL(project.image, Astro.site).href : undefined} ogImageAlt={project.titleEs}>
  <main id="main-content" class="relative z-10 min-h-screen">
    <div class="max-w-5xl mx-auto px-4 py-24 md:py-32">
      <AnimatedSection client:load className="mb-8">
        <a
          href="/#projects"
          class="inline-flex items-center gap-2 text-sm text-muted hover:text-accent transition-colors font-mono group"
        >
          <svg class="w-4 h-4 transition-transform group-hover:-translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
          </svg>
          <span data-lang="es">Volver a proyectos</span>
          <span data-lang="en" hidden>Back to projects</span>
        </a>
      </AnimatedSection>

      <AnimatedSection client:load delay={0.05}>
        <div class="relative bg-surface-elevated/60 backdrop-blur-sm border border-border rounded-2xl overflow-hidden">
          <div class={`absolute left-0 top-0 bottom-0 w-1.5 bg-gradient-to-b ${project.color} z-10`} />
          {project.iframe ? (
            <div class="relative w-full" style="aspect-ratio: 16/9;" transition:name={`${slug}-media`}>
              <iframe
                src={project.iframe}
                title={project.titleEs}
                class="w-full h-full border-0"
                loading="lazy"
                sandbox="allow-scripts"
                allow="geolocation 'none'"
                style="pointer-events: none;"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-surface-elevated via-surface-elevated/10 to-transparent pointer-events-none" />
            </div>
          ) : (
            <div class="relative w-full" style="aspect-ratio: 16/9;" transition:name={`${slug}-media`}>
              <img
                src={project.image}
                alt={project.titleEs}
                class="w-full h-full object-cover object-top"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-surface-elevated via-surface-elevated/10 to-transparent pointer-events-none" />
            </div>
          )}

          <div class="p-8 md:p-12">
            <div class="flex flex-wrap gap-2 mb-5">
              {project.tags.map((tag) => (
                <span class="text-xs px-3 py-1.5 rounded-full bg-accent/15 text-primary border border-accent/10 font-medium tracking-wide">{tag}</span>
              ))}
            </div>
            <h1 class="text-3xl md:text-5xl font-bold text-primary mb-6 leading-tight" transition:name={`${slug}-title`}>
              <span data-lang="es">{project.titleEs}</span>
              <span data-lang="en" hidden>{project.titleEn}</span>
            </h1>
            <p class="text-secondary text-lg md:text-xl leading-relaxed max-w-3xl">
              <span data-lang="es">{project.longDescEs}</span>
              <span data-lang="en" hidden>{project.longDescEn}</span>
            </p>

            {project.metrics && project.metrics.length > 0 && (
              <div class="mt-8 grid grid-cols-3 gap-4">
                {project.metrics.map((m) => (
                  <div class="bg-surface/40 border border-border/30 rounded-xl p-4 text-center">
                    <div class="text-2xl md:text-3xl font-bold text-accent">{m.value}</div>
                    <div class="text-xs text-secondary mt-1">
                      <span data-lang="es">{m.labelEs}</span>
                      <span data-lang="en" hidden>{m.labelEn}</span>
                    </div>
                  </div>
                ))}
              </div>
            )}

            {project.testimonials && project.testimonials.length > 0 && (
              <div class="mt-10 p-8 rounded-2xl bg-gradient-to-br from-accent/5 to-accent-secondary/5 border border-accent/10 relative overflow-hidden">
                <div class={`absolute top-0 left-0 right-0 h-1.5 bg-gradient-to-r ${project.color}`} />
                <svg class="w-10 h-10 text-accent/20 mb-4" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
                </svg>
                <p class="text-xl md:text-2xl text-primary font-light leading-relaxed italic mb-2">
                  <span data-lang="es">{project.testimonials[0].textEs}</span>
                  <span data-lang="en" hidden>{project.testimonials[0].textEn}</span>
                </p>
                <p class="text-sm text-muted font-medium">
                  — {project.testimonials[0].author}{project.testimonials[0].role ? `, ${project.testimonials[0].role}` : ''}
                </p>
              </div>
            )}

            {project.links && project.links.length > 0 && (
              <div class="flex flex-wrap gap-3 mt-8">
                {project.links.map((link) => (
                  <a
                    href={link.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl bg-accent/15 text-primary text-sm font-medium hover:bg-accent/25 transition-all border border-accent/10"
                  >
                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                    </svg>
                    <span data-lang="es">{link.labelEs}</span>
                    <span data-lang="en" hidden>{link.labelEn}</span>
                  </a>
                ))}
              </div>
            )}
          </div>
        </div>
      </AnimatedSection>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mt-12">
        <div class="lg:col-span-2 space-y-10">
          <AnimatedSection client:load delay={0.15}>
            <div class="bg-surface-elevated/40 backdrop-blur-sm border border-border/50 rounded-2xl p-8">
              <h2 class="text-xl font-bold text-primary mb-6 flex items-center gap-3">
                <span class={`w-1.5 h-6 rounded-full bg-gradient-to-b ${project.color}`} />
                <span data-lang="es">Características</span>
                <span data-lang="en" hidden>Features</span>
              </h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                {project.featuresEs.map((feat, i) => (
                  <div class="flex items-start gap-3 p-4 rounded-xl bg-surface/40 border border-border/30 hover:bg-surface/60 transition-colors">
                    <div class={`w-2 h-2 mt-2.5 rounded-full bg-gradient-to-b ${project.color} shrink-0`} />
                    <span class="text-secondary text-sm leading-relaxed">
                      <span data-lang="es">{feat}</span>
                      <span data-lang="en" hidden>{project.featuresEn[i]}</span>
                    </span>
                  </div>
                ))}
              </div>
            </div>
          </AnimatedSection>

          {project.costImage && (
            <AnimatedSection client:load delay={0.2}>
              <div class="bg-surface-elevated/40 backdrop-blur-sm border border-border/50 rounded-2xl p-8">
                <h2 class="text-xl font-bold text-primary mb-6 flex items-center gap-3">
                  <span class={`w-1.5 h-6 rounded-full bg-gradient-to-b ${project.color}`} />
                  <span data-lang="es">Impacto</span>
                  <span data-lang="en" hidden>Impact</span>
                </h2>
                <div class="rounded-xl overflow-hidden border border-border/30">
                  <img src={project.costImage} alt="Reducción de costos de IA" class="w-full" loading="lazy" />
                </div>
                <p class="text-sm text-muted mt-4">
                  <span data-lang="es">Reducción de costos de IA de &gt;$200/mes a $0 implementando Cursor Rules.</span>
                  <span data-lang="en" hidden>AI cost reduction from &gt;$200/mo to $0 by implementing Cursor Rules.</span>
                </p>
              </div>
            </AnimatedSection>
          )}

          {project.testimonials && project.testimonials.length > 1 && project.testimonials.slice(1).map((t) => (
            <AnimatedSection client:load delay={0.25}>
              <div class="bg-surface-elevated/40 backdrop-blur-sm border border-border/50 rounded-2xl p-8 relative overflow-hidden">
                <div class={`absolute top-0 left-0 right-0 h-1 bg-gradient-to-r ${project.color}`} />
                <svg class="w-8 h-8 text-accent/20 mb-4" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
                </svg>
                <p class="text-base text-primary italic leading-relaxed mb-2">
                  <span data-lang="es">{t.textEs}</span>
                  <span data-lang="en" hidden>{t.textEn}</span>
                </p>
                <p class="text-xs text-muted font-medium">
                  — {t.author}{t.role ? `, ${t.role}` : ''}
                </p>
              </div>
            </AnimatedSection>
          ))}
        </div>

        <div class="space-y-6">
          <AnimatedSection client:load delay={0.15}>
            <div class="bg-surface-elevated/40 backdrop-blur-sm border border-border/50 rounded-2xl p-6">
              <h3 class="text-xs font-semibold text-muted uppercase tracking-wider mb-4">
                <span data-lang="es">Stack Tecnológico</span>
                <span data-lang="en" hidden>Tech Stack</span>
              </h3>
              <div class="flex flex-wrap gap-2">
                {project.tags.map((tag) => (
                  <span class="text-xs px-3 py-1.5 rounded-lg bg-surface/50 text-secondary border border-border/30 font-medium">{tag}</span>
                ))}
              </div>
            </div>
          </AnimatedSection>

          {project.links && project.links.length > 0 && (
            <AnimatedSection client:load delay={0.2}>
              <div class="bg-surface-elevated/40 backdrop-blur-sm border border-border/50 rounded-2xl p-6">
                <h3 class="text-xs font-semibold text-muted uppercase tracking-wider mb-4">
                  <span data-lang="es">Enlaces</span>
                  <span data-lang="en" hidden>Links</span>
                </h3>
                <div class="space-y-2">
                  {project.links.map((link) => (
                    <a
                      href={link.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="flex items-center gap-2 text-sm text-accent hover:text-accent-secondary transition-colors"
                    >
                      <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                      </svg>
                      <span data-lang="es">{link.labelEs}</span>
                      <span data-lang="en" hidden>{link.labelEn}</span>
                    </a>
                  ))}
                </div>
              </div>
            </AnimatedSection>
          )}
        </div>
      </div>
    </div>
  </main>
</Layout>
